{
  "name": "local-codex-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Workbench CLI for macOS with Ollama integration.",
  "bin": {
    "app": "./src/cli.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "test": "node --test",
    "models:list": "node ./src/cli.js models list"
  },
  "engines": {
    "node": ">=20"
  }
}
