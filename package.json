{
  "name": "local-codex-agent",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Workbench CLI for macOS with local and remote LLM provider support.",
  "bin": {
    "app": "./src/cli.js",
    "workbench": "./src/workbench.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "test": "node --test",
    "models:list": "node ./src/cli.js models list"
  },
  "engines": {
    "node": ">=20"
  }
}
